# Workstation setup dry run

This file was added by an automated end-to-end test of the local agent
workflow (worktree -> change -> verify -> commit -> push -> PR). It is safe
to delete; it does not touch any application code.
